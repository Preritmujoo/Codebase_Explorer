[pytest]
testpaths = tests
pythonpath = .
# also allow running from repo root: if pytest is invoked from project root,
# the `backend` package is found via the root path; `pythonpath = .` then
# resolves inside backend/ when cwd=backend, and tests have fallback imports
# for `backend.app...` when run from root.
